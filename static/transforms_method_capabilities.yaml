resize_and_crop:
  capability:
    - resize by width
    - resize by height
    - responsive resize auto dimension
    - arithmetic expression based resize
    - aspect ratio based resize
    - aspect ratio enforced crop
    - crop force resize
    - crop at least resize
    - crop at max enlarge resize
    - crop maintain ratio resize
    - extract crop
    - padded extract crop
    - padded resize
    - add padding while resizing an image to specific dimensions
    - intelligent focus auto
    - intelligent focus face detection
    - intelligent focus object detection
    - positional focus padding control
    - positional focus relative crop
    - custom focus region
    - zoom based cropping
    - absolute coordinate crop
    - center based coordinate crop
    - device pixel ratio scaling

ai_transform:
  capability:
    # Background & foreground manipulation
    - remove background using AI (external provider) Removedotbg, remove dot, bg remove.bg, remove dot bg
    - remove background using ImageKit native AI
    - remove background with preserved foreground subject
    - change image background using text prompt
    - reposition foreground subject while changing background
    - preserve subject while replacing background context using prompt
    - free form of changing background using natural language

    # Image editing & modification
    - edit image content using natural language prompt
    - modify image appearance using base64 encoded prompt
    - object-aware image editing
    - context-aware image modification

    # Generative image expansion
    - generative fill beyond original image boundaries
    - AI-based outpainting with size-controlled canvas
    - prompt-driven generative fill
    - resize-aware generative background extension
    - crop-mode dependent generative fill

    # Visual effects
    - generate AI-based drop shadow around subject
    - control light direction for drop shadow
    - control elevation of light source
    - control shadow intensity
    - apply drop shadow to transparent images only

    # Image enhancement
    - retouch image to improve visual quality
    - remove blemishes and visual artifacts using AI
    - upscale low resolution images using AI
    - super-resolution upscaling to fixed maximum resolution

    # Image generation
    - generate new image variations from existing image
    - controlled variation generation while preserving structure
    - regenerate image by cache-busting or path change

    # Encoding & prompt handling
    - plain text prompt support
    - base64 encoded prompt support
    - safe handling of special characters in prompts

image_overlay:
  capability:
    # Overlay composition
    - overlay an image on top of a base image
    - layer multiple image overlays sequentially
    - use solid color blocks as overlays
    - use gradient-filled overlays
    - nest overlays within other overlays

    # Positioning & layout
    - position overlay using absolute coordinates
    - position overlay using relative focus points
    - position overlay using arithmetic expressions
    - position overlay using negative offsets
    - align overlay relative to parent container
    - control overlay placement using focus anchors

    # Sizing & cropping
    - resize overlay by width
    - resize overlay by height
    - resize overlay using arithmetic expressions
    - crop overlay using extract mode
    - pad and resize overlay to fixed dimensions
    - apply padded extract crop
    - define exact crop region using absolute coordinates
    - define crop region using center-based coordinates
    - zoom into cropped region
    - enforce crop behavior using crop modes

    # Background handling
    - apply solid color background behind overlay
    - apply dominant color background
    - apply blurred image background
    - apply gradient background behind overlay
    - control background appearance during padding
    - support transparent overlays with background fill

    # Visual effects on overlays
    - convert overlay to grayscale
    - apply border around overlay
    - automatically trim transparent edges
    - add rounded corners to overlay
    - apply blur to overlay
    - rotate overlay image
    - flip overlay horizontally or vertically
    - add drop shadow to overlay image
    - replace colors inside overlay image

    # Layer blending & masking
    - blend overlay with base image using multiply mode
    - cut transparent regions into base image using cutout mode
    - mask base image using overlay shape using cutter mode
    - distort base image using overlay as displacement map

    # Encoding & control
    - support encoded overlay image paths
    - support arithmetic expressions for all numeric parameters
    - support negative values via internal encoding

text_overlay:
  capability:
    # Text rendering
    - render text as an overlay on images
    - render multi-line wrapped text
    - auto-calculate text height based on content
    - constrain text within maximum renderable dimensions

    # Positioning & layout
    - position text using absolute coordinates
    - position text using arithmetic expressions
    - position text using negative offsets
    - align text layer relative to base image
    - align text relative to parent container
    - control text placement using focus anchors

    # Typography & font control
    - control font size using numbers or expressions
    - set custom font family
    - use predefined font families
    - apply bold, italic, and strikethrough styles
    - combine multiple typography styles
    - control line height for wrapped text
    - control text alignment within text box

    # Color & background
    - set text color using named colors
    - set text color using RGB hex
    - set text color using RGBA hex with opacity
    - apply solid background behind text
    - apply padding around text content
    - control background transparency using alpha

    # Transformations & effects
    - flip text horizontally or vertically
    - rotate text layer
    - blur text layer
    - apply rounded corners to text background
    - trim text background automatically

    # Layer blending & masking
    - blend text with base image using multiply mode
    - cut transparent text shapes into base image
    - mask base image using text shape
    - use text as a cutter mask for base image

    # Expression & limits
    - support arithmetic expressions for size and position
    - serialize negative values using internal encoding
    - enforce text rendering size limits

effects_and_enhancement:
  capability:
    # Image enhancement
    - automatically enhance image contrast
    - sharpen images using basic sharpening
    - apply advanced unsharp masking
    - convert images to grayscale
    - improve visual clarity and edge definition

    # Blur & distortion
    - apply gaussian blur with configurable intensity
    - distort image perspective using custom coordinates
    - curve images using arc distortion
    - displace pixels using displacement maps

    # Color & tone manipulation
    - replace specific colors while preserving luminosity
    - control tolerance for color replacement
    - adjust color appearance using gradients
    - apply linear gradient overlays
    - apply gradient backgrounds behind layers
    - apply gradient backgrounds using color gradients, can specify start and end colors

    # Shadow & depth
    - add drop shadow to transparent images
    - control shadow blur, saturation, and offsets
    - simulate elevation using shadow parameters

    # Geometry & orientation
    - rotate images clockwise or counter-clockwise
    - auto-rotate images using EXIF data
    - flip images horizontally or vertically
    - apply rounded corners or circular masking

    # Trimming & cleanup
    - automatically trim uniform backgrounds
    - control trim aggressiveness using threshold
    - remove excess transparent or solid regions

    # Chaining & scope
    - apply effects globally to base image
    - apply effects to image overlays
    - apply effects to text overlays
    - chain multiple effects in a single transformation
